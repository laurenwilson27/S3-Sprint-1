<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BitBase Membership</title>
    <link rel="stylesheet" href="/resources/style.css" />
  </head>
  <body>
    <header>
      <h1>BITBASE</h1>
    </header>
    <hr width="100%" />
    <div class="mainPage">
      <% if (token.status == "ok") { %>
      <div class="success">
        Token was successfully created! <% token.status %>
      </div>
      <p>
        The token for new user <%= token.username %> is
        <span class="token"><%= token.token %></span>
      </p>
      <% } %> <% if (token.status == "error") { %>
      <div class="error">There was an error with token creation</div>
      <p><%= token.message %></p>
      <% } %>
    </div>
  </body>
</html>
